
allow hal_fingerprint_default tee:unix_stream_socket { connectto };
allow tee hal_fingerprint_default:dir { search };
allow tee hal_fingerprint_default:file { open read getattr };
allow tee hal_fingerprint_default:process { getattr };
allow hal_fingerprint_default tee:fd { use };
allow hal_fingerprint_default sysfs:file { write };

